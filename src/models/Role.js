export default class Role {
    constructor(countId, id, name) {
        this.CountId = countId;
        this.Id = id;
        this.Name = name;
    };

    CountId = -1;
    Id = '';
    Name = '';
};